<%= form_for(@pog) do |f| %>
  <% if @pog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pog.errors.count, "error") %> prohibited this pog from being saved:</h2>

      <ul>
      <% @pog.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
